In this directory we put files that are needed to test some things inside the OS.
Currently, with a script in scripts dir we generate a fat32 filesystem image with
dummy files and directories. It is used to test file system classes inside the
image.
